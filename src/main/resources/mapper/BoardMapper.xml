<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.infected_requiem.requiem.repository.BoardMapper">
    <!-- 게시글 목록 조회 -->
    <select id="findPage" resultType="com.infected_requiem.requiem.dto.log.PostDTO">
        SELECT
            post_id AS postId,
            tag_id AS tagId,
            post_title AS postTitle,
            post_content AS postContent,
            post_date AS postDate
        FROM post_tbl
        <where> <if test="tagId != null"> tag_id = #{tagId} </if> </where>
        ORDER BY post_id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 카운팅 -->
    <select id="countPost" resultType="int">
        SELECT COUNT(*)
        FROM post_tbl
        <where> <if test="tagId != null"> tag_id = #{tagId} </if> </where>
    </select>

    <!-- 게시글 등록 -->
    <insert id="insertPost" parameterType="com.infected_requiem.requiem.dto.log.PostDTO" useGeneratedKeys="true" keyProperty="postId">
        INSERT INTO post_tbl (tag_id, post_title, post_content) VALUES (#{tagId}, #{postTitle}, #{postContent})
    </insert>

    <!-- 게시글 단일 조회 -->
    <select id="findById" parameterType="int" resultType="com.infected_requiem.requiem.dto.log.PostDTO">
        SELECT
            post_id AS postId,
            tag_id AS tagId,
            post_title AS postTitle,
            post_content AS postContent,
            post_date AS postDate
        FROM post_tbl
        WHERE post_id = #{postId}
    </select>

    <!-- 게시글 수정 -->
    <update id="updatePost" parameterType="com.infected_requiem.requiem.dto.log.PostDTO">
        UPDATE post_tbl
            SET tag_id = #{tagId}, post_title = #{postTitle}, post_content = #{postContent}
        WHERE post_id = #{postId}
    </update>

    <!-- 게시글 삭제 -->
    <delete id="deleteById" parameterType="int">
        DELETE FROM post_tbl WHERE post_id = #{postId}
    </delete>
</mapper>