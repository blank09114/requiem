<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.infected_requiem.requiem.repository.UserMapper">
    <!-- 로그인 -->
    <select id="selectUserById" resultType="com.infected_requiem.requiem.dto.user.UserDTO">
        SELECT
            user_id AS userId,
            user_pw AS userPw,
            user_name AS userName,
            user_role AS userRole,
            is_block AS isBlock
        FROM user_tbl
        WHERE user_id = #{userId}
    </select>

    <!-- 회원가입 -->
    <insert id="insertUser">
        INSERT INTO user_tbl ( user_id, user_pw, user_name )
        VALUES ( #{userId}, #{userPw}, #{userName} )
    </insert>

    <!-- ID 중복 체크 -->
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM user_tbl WHERE user_id = #{userId}
    </select>
</mapper>